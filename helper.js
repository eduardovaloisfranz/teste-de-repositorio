/**
 * @example
 * Funcão que recebe um número e retorna se é par ou não
 * @param {number} number numero a ser calculado se é par ou não
 * @returns {boolean}
 */
export const isEven = (number) => {
    return number %2 === 0
}
